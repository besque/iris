"""Reads the query, picks tools, runs them, collects the trace."""


def handle_query(query: str, validated_inputs: dict) -> dict:
    """Main entry point. Returns answer, evidence, confidence, trace."""
    raise NotImplementedError
