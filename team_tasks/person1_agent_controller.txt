ROLE: Agent Controller + Integration Lead (you own the "brain" and the final glue)

You are the most important person for the demo working end to end. Everyone else builds tools, you make sure they plug in.

WHAT TO BUILD (in backend/agent/):
1. controller.py: takes the user query + validated input info, decides which task it is (vqa / caption / grounding / change / fusion), picks tools from registry.py, runs them, collects results.
2. registry.py: the fixed menu of tools. Each entry has a name, description, and what inputs it accepts.
3. Execution trace: for every query, record task chosen, tools used, params, and outputs as a dict. Judges evaluate this, so do it from the start, not at the end.

HOW TO DECIDE THE APPROACH (research 30-45 min max, then commit to one):
- Look at how Remote-Sensing-ChatGPT does routing: https://github.com/HaonanGuo/Remote-Sensing-ChatGPT (simplest controller loop, copy the pattern)
- Also skim RS-Agent: https://github.com/intellisensing/rs-agent
- Decision you own: use an LLM API (GPT/Claude with function calling) as the router, or a simple rule-based router (keywords + input type). Given one day, recommendation: LLM function calling with a rule-based fallback. You decide.

KEY TRICK FOR TODAY:
Write 5 MOCK tools first (fake VQA, fake caption, etc. that return dummy ToolResult objects). Get the whole pipeline working query -> route -> tool -> trace -> response with mocks by early afternoon. Then swap mocks for real tools as others finish. Never wait on anyone.

DEPENDENCIES:
- Everyone's tools follow backend/tools/base.py (Tool class, ToolResult). This file is the contract. If anyone wants to change it, they must tell the whole group. You are the owner of this file.
- Person 4 gives you the input config types (single_optical, single_sar, bitemporal_pair, crossmodal_pair). Until then, hardcode a fake validated_inputs dict.
- Person 5 calls your handle_query() from the API. Agree with them on the exact return dict shape in the first hour (answer, spatial, confidence, trace). Write it in docs/ARCHITECTURE.md.

BY END OF DAY:
- handle_query() works with all real tools that landed, mocks for any that did not.
- Execution trace visible in the response.
- Update docs/ARCHITECTURE.md with the routing decision you made.
