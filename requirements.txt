# Runtime (web app + API + tools). Training extras are in requirements-train.txt.
fastapi
uvicorn[standard]
pydantic
python-multipart   # file uploads in FastAPI
requests           # tools calling the model endpoint
rasterio           # read GeoTIFF/TIFF
numpy
pillow

# Dev
pytest
