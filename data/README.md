# Data (gitignored)

Datasets live here locally but are never committed.
Use the download scripts in `scripts/` so everyone has the same layout:

```
data/
├── bigearthnet/
├── rsvqa/
├── vrsbench/
├── cdvqa/
└── samples/        # a few small demo images for quick testing
```
